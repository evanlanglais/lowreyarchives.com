# Supabase — required at build time (baked into SPA bundle) and runtime.
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SECRET_KEY=

# S3 / Garage — runtime.
NUXT_S3_KEY=
NUXT_S3_KEY_SECRET=

# nuxt-multi-cache invalidation API token.
NUXT_MULTI_CACHE_API_TOKEN=

# Cloudflare Tunnel token — from Zero Trust dashboard → Networks → Tunnels.
TUNNEL_TOKEN=
